# CyberMirror v2.5.1 integration status

This document records the first dependency-safe batch from roadmap #68. It does not claim that the complete roadmap is finished.

## Implemented in this batch

- Central public-target validator shared by HTTP and Playwright acquisition.
- Manual redirect validation and structured unsafe-target outcomes.
- Rendered login/CAPTCHA/error state preservation.
- Production intelligence acquisition for URL-only findings with snippet fallback.
- Scan cancellation propagation into the active investigation budget and registry cleanup.
- Explicit evidence-to-artifact-to-cluster lineage mapping.
- Mirror-safe identity corroboration and timeline counts.
- Identity-aware travel derivation after clustering, with mirror travel suppression.
- Journal steps for search results, entity extraction, identity/travel conclusions, and traversable parent relationships.

## Still open on roadmap #68

- Execute structured initial query plans and bounded contextual pivot searches (#58).
- Expand structured/semantic extraction and multi-author activity coverage (#61).
- Complete public cross-link and contradiction propagation (#59).
- Finish Timeline filters/navigation, graph semantics/UI, and richer reports/exports (#63–#65).
- Add the final two-depth real-orchestrator release-gate scenario after recursive search is wired (#67).

All fixtures and tests use synthetic identifiers and mocked external boundaries.
